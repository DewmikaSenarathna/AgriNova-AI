# Core (required)
sentence-transformers>=2.7.0   # embeds the farmer's question (BGE model)
chromadb>=0.5.0                 # reads the vector database built by Phase 4/5
requests>=2.31.0                # talks to the LLM backend (Ollama / Groq / OpenAI-compatible)
python-dotenv>=1.0.0            # loads LLM_PROVIDER etc. from a local .env file

# API server (required to run api.py)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# sentence-transformers pulls these in automatically, listed here for clarity:
# torch, numpy, transformers, huggingface-hub
